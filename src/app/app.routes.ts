import { Routes } from '@angular/router';
import { KanbanComponents } from './features/kanban/components/kanban.components';

export const routes: Routes = [{ path: 'kanban', component: KanbanComponents }];
